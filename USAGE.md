# Mini Video Agent 使用指南（给使用者）

用中文对话剪本地视频：上传 → 下指令 → 确认 → 看预览。  
当前版本：**v0.5**（本地体验站，不收费、不上公网）。

| 文档 | 用途 |
|------|------|
| [README.md](./README.md) | 项目总览 |
| [PROBLEMS.md](./PROBLEMS.md) | **排障与历史坑**（VPN、改名、缓存等） |
| [HANDOFF.md](./HANDOFF.md) | 给下一个开发者 / Agent 的交接 |
| [CHANGELOG.md](./CHANGELOG.md) | 版本变更 |
| [LEARNING.md](./LEARNING.md) | Agent 原理学习 |

**重要：** 请用项目 `.venv` 启动；国内访问 Gemini 时，浏览器开 VPN 往往不够，请配置 `HTTPS_PROXY` 或系统代理（详见 PROBLEMS）。页面改版后请 **Ctrl+F5**。

---

## 你能做什么

| 你想说的 | 效果 |
|----------|------|
| 这个视频多长？ | 看时长、分辨率等 |
| 去掉前 3 秒 | 丢掉片头，保留后面 |
| 只保留 2 秒到 8 秒 | 只要中间一段 |
| 删掉中间 2 秒到 4 秒 | 中间挖掉，前后拼回 |
| 加标题：决赛高光 | 画面上叠大标题 |
| 底部加字幕：感谢观看 | 底部字幕 |
| 右上角贴纸：HIT | 角标 |
| 静音 / 两倍速 / 慢放一半 | 音频或速度 |
| 把 a.mp4 和 b.mp4 拼起来 | 多段拼接 |
| 截第 1 秒看看 | 截一帧预览图 |
| 打开 / 看看效果 | 网页里播放最新成片 |
| 列出已导出的视频 | 看 `output/` 成片列表 |
| 改名叫拼接 / 命名为 xxx | 重命名最新成片（也可网页点「改名」） |
| 把 A 和 B 拼起来，命名为拼接 | 拼接后自动进入改名（确认后执行） |

会改文件的操作都会**先给计划**；网页会出现 **确认执行 / 取消** 按钮，也可以输入「确认」。

---

## 第一次使用（约 5 分钟）

### 1. 环境准备

- Windows / macOS / Linux，已装 **Python 3.10+**
- 能访问 Google Gemini（国内通常需要 **VPN / 代理**）
- [申请 Gemini API Key](https://aistudio.google.com/apikey)

### 2. 安装

在项目目录打开终端：

```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
```

macOS / Linux 把激活改成 `source .venv/bin/activate`，复制改成 `cp .env.example .env`。

编辑 `.env`，至少填好：

```env
GEMINI_API_KEY=你的密钥
GEMINI_MODEL=gemini-flash-lite-latest
```

如需代理，可加：

```env
HTTPS_PROXY=http://127.0.0.1:7890
```

（端口改成你自己的代理端口。）

### 3. 启动网页（推荐）

```powershell
.\.venv\Scripts\activate
python web_app.py
```

浏览器打开：**http://127.0.0.1:7860**

看到「Mini Video Agent」页面即成功。结束服务：终端里按 `Ctrl+C`。

仓库自带默认样本 **`samples/demo.mp4`**（未上传时，CLI / 工具会用它作工作视频）。网页里也可直接 **上传** 自己的片覆盖。

### 4. 第一次试剪（推荐路径）

1. （可选）点 **上传视频**，或直接用默认样本开始对话  
2. 右侧聊天区输入或点快捷按钮：
   - `这个视频多长？`
   - `去掉前 3 秒`
   - 出现【待确认】后点 **确认执行**（或输入 `确认`）；不想改就点 **取消**
   - 成片会出现在播放器与「成片列表」；也可说 `截第 1 秒看看`
3. 成片文件在项目的 `output/` 目录，可用资源管理器拷走
4. 点 **对话记录** 可回看本机保存的历史对话

---

## 页面怎么用

```text
┌──────────────────────────────────────────────┐
│  Mini Video Agent                            │
├───────────────────────┬──────────────────────┤
│  播放器               │  对话区               │
│  成片列表（点选播放）  │  确认按钮 / 快捷指令   │
│  截帧预览图           │                      │
│  上传 · 记录 · 清空   │                      │
└───────────────────────┴──────────────────────┘
```

- **上传视频**：设为当前「工作视频」，并清空模型上下文（对话记录仍保留，除非你点清空）  
- **刷新预览**：重新拉取最新成片 / 截图  
- **对话记录**：按**日期 / 会话**浏览历史（左侧目录切换；约 3 小时无消息会自动开新会话）  
- **新对话**：清当前聊天上下文并开新会话；旧会话仍可在记录里查看，**不删**成片  
- **成片列表**：点击播放；每条可 **改名**、看 **位置**（打开资源管理器并尽量复制路径）  
- **打开文件夹**：直接打开 `output/` 目录  
- **截帧预览**：静止画面，图片下方标注 **第 N 秒**；点击可放大  
- **确认执行 / 取消**：待确认计划出现时显示  
- **快捷按钮**：多长？ / 去片头 / 截帧 / 重试上一条  

剪辑过程中**尽量不要刷新浏览器**，否则可能看不到当次回复（后台可能仍在跑）。对话记录仍可在「对话记录」里查看。

---

## 常用说法（可直接复制）

**切片**

```text
去掉前 5 秒
只保留 1 秒到 6 秒
切得更准一点（需要时再说）
```

**删中间 / 拼接**

```text
删掉中间 2 秒到 4 秒
把 trim_0_4_xxxx.mp4 和另一段拼起来
```

**文字**

```text
加标题：今日高光
底部加字幕：感谢观看
右上角贴纸：HIT
```

**音画**

```text
静音
两倍速
慢放一半
```

**预览与管理**

```text
截第 1 秒看看
打开刚导出的视频
列出已导出的视频
删除 trim_0_4_xxxxxxxx.mp4
```

最后一条删除也要先确认；**只能删导出成片**，不会删你的原片。

---

## 命令行用法（可选）

不想开网页时：

```powershell
.\.venv\Scripts\activate
python main.py
```

默认使用 `samples/demo.mp4`（也可在 `.env` 改 `DEFAULT_VIDEO=`）。  
对话方式与网页相同；「打开」会用系统默认播放器。

---

## 文件都在哪

| 位置 | 内容 |
|------|------|
| `uploads/` | 网页上传的视频 |
| `output/` | 切片、加字、拼接等导出成片 |
| `output/previews/` | 截帧 PNG |
| `samples/demo.mp4` | 仓库自带默认样本（约数秒短片） |
| `data/chat_sessions.json` | 多会话对话记录（本机，勿提交隐私内容） |
| `.env` | API Key 等私密配置（不要发给别人） |

---

## 常见问题

更完整的踩坑记录（含原因与修复过程）见 **[PROBLEMS.md](./PROBLEMS.md)**。

**一直停在「思考并调用工具中…」或 ConnectTimeout / 10060**  
不是指令写错，是 **Python 连不上 Google**。请开系统代理/TUN，或在 `.env` 设 `HTTPS_PROXY` 后重启。详见 PROBLEMS §3。

**`No module named 'fastapi'`**  
请先 `.\.venv\Scripts\Activate.ps1`，不要用裸的 Anaconda `(base)`。

**端口被占用（WinError 10048）**  

```powershell
$env:WEB_PORT=7861; python web_app.py
```

**终端 ConnectionResetError / 10054**  
拖视频进度条时常见，**可忽略**。

**没有确认按钮 / 成片列表空白 / 对话记录没反应**  
先 **Ctrl+F5**，再点「刷新预览」；仍不行则重启 `web_app.py`（多为缓存旧页面）。

**改名变成 `1.mp4`（本想叫 1.5倍速）**  
旧 bug，已修；请用最新代码并重启后再改名。

**429 / 503**  
换 `GEMINI_MODEL=gemini-flash-lite-latest` 或稍后再试。

**中文叠字方框**  

```env
VIDEO_FONT=C:\Windows\Fonts\msyh.ttc
```

**切片时间差一点**  
说「切得更准一点」。

**上传多大？**  
默认约 100MB（`WEB_MAX_UPLOAD_MB`）。

---

## 使用注意

- 本机单人体验：没有账号系统，也不适合直接暴露到公网  
- 改文件前一定有「确认」步骤；不确定就先别确认  
- API Key、个人视频不要提交到 Git  
- 依赖在线 Gemini，免费额度有限；网络不稳时回复会变慢  

---

## 下一步可以玩什么

1. 上传一条竖屏短视频，做「去片头 → 加标题 → 截帧」  
2. 切两段再「拼起来」  
3. 试「静音 + 两倍速」看节奏变化  

玩顺之后，若想了解背后的 Agent / 工具调用，再看 [LEARNING.md](./LEARNING.md)。
