# Mini Video Agent 使用指南（给使用者）

用中文对话剪本地视频：上传 → 下指令 → 确认 → 看预览。  
当前版本：**v0.4**（本地体验站，不收费、不上公网）。

开发者 / 版本记录请看 [README.md](./README.md)、[CHANGELOG.md](./CHANGELOG.md)；学 Agent 原理请看 [LEARNING.md](./LEARNING.md)。

---

## 你能做什么

| 你想说的 | 效果 |
|----------|------|
| 这个视频多长？ | 看时长、分辨率等 |
| 去掉前 3 秒 | 丢掉片头，保留后面 |
| 只保留 2 秒到 8 秒 | 只要中间一段 |
| 删掉中间 2 秒到 4 秒 | 中间挖掉，前后拼回 |
| 加标题：决赛高光 | 画面上叠大标题 |
| 底部加字幕：感谢观看 | 底部字幕 |
| 右上角贴纸：HIT | 角标 |
| 静音 / 两倍速 / 慢放一半 | 音频或速度 |
| 把 a.mp4 和 b.mp4 拼起来 | 多段拼接 |
| 截第 1 秒看看 | 截一帧预览图 |
| 打开 / 看看效果 | 网页里播放最新成片 |
| 列出已导出的视频 | 看 `output/` 成片列表 |

会改文件的操作都会**先给计划**，你再说「确认」才会真正导出。

---

## 第一次使用（约 5 分钟）

### 1. 环境准备

- Windows / macOS / Linux，已装 **Python 3.10+**
- 能访问 Google Gemini（国内通常需要 **VPN / 代理**）
- [申请 Gemini API Key](https://aistudio.google.com/apikey)

### 2. 安装

在项目目录打开终端：

```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
```

macOS / Linux 把激活改成 `source .venv/bin/activate`，复制改成 `cp .env.example .env`。

编辑 `.env`，至少填好：

```env
GEMINI_API_KEY=你的密钥
GEMINI_MODEL=gemini-flash-lite-latest
```

如需代理，可加：

```env
HTTPS_PROXY=http://127.0.0.1:7890
```

（端口改成你自己的代理端口。）

### 3. 启动网页（推荐）

```powershell
.\.venv\Scripts\activate
python web_app.py
```

浏览器打开：**http://127.0.0.1:7860**

看到「Mini Video Agent」页面即成功。结束服务：终端里按 `Ctrl+C`。

仓库自带默认样本 **`samples/demo.mp4`**（未上传时，CLI / 工具会用它作工作视频）。网页里也可直接 **上传** 自己的片覆盖。

### 4. 第一次试剪（推荐路径）

1. （可选）点 **上传视频**，或直接用默认样本开始对话  
2. 右侧聊天区输入或点快捷按钮：
   - `这个视频多长？`
   - `去掉前 3 秒`
   - 等助手给出【待确认】计划后，点快捷 **确认**（或输入 `确认`）
   - 成片会出现在左侧播放器；也可说 `截第 1 秒看看`
3. 成片文件在项目的 `output/` 目录，可用资源管理器拷走

---

## 页面怎么用

```text
┌─────────────────────────────────────────────┐
│  Mini Video Agent                           │
├──────────────────────┬──────────────────────┤
│  播放器 / 截帧条      │  对话区               │
│  上传 · 刷新 · 清空   │  输入指令 · 快捷按钮   │
└──────────────────────┴──────────────────────┘
```

- **上传视频**：设为当前「工作视频」，并清空本轮对话上下文  
- **刷新预览**：重新拉取最新成片 / 截图  
- **清空对话**：只清聊天记录，不删成片  
- **快捷按钮**：多长？ / 去片头 / 确认 / 截帧  
- **截帧条**：点缩略图可放大查看  

剪辑过程中**尽量不要刷新浏览器**，否则可能看不到当次回复（后台可能仍在跑）。

---

## 常用说法（可直接复制）

**切片**

```text
去掉前 5 秒
只保留 1 秒到 6 秒
切得更准一点（需要时再说）
```

**删中间 / 拼接**

```text
删掉中间 2 秒到 4 秒
把 trim_0_4_xxxx.mp4 和另一段拼起来
```

**文字**

```text
加标题：今日高光
底部加字幕：感谢观看
右上角贴纸：HIT
```

**音画**

```text
静音
两倍速
慢放一半
```

**预览与管理**

```text
截第 1 秒看看
打开刚导出的视频
列出已导出的视频
删除 trim_0_4_xxxxxxxx.mp4
```

最后一条删除也要先确认；**只能删导出成片**，不会删你的原片。

---

## 命令行用法（可选）

不想开网页时：

```powershell
.\.venv\Scripts\activate
python main.py
```

默认使用 `samples/demo.mp4`（也可在 `.env` 改 `DEFAULT_VIDEO=`）。  
对话方式与网页相同；「打开」会用系统默认播放器。

---

## 文件都在哪

| 位置 | 内容 |
|------|------|
| `uploads/` | 网页上传的视频 |
| `output/` | 切片、加字、拼接等导出成片 |
| `output/previews/` | 截帧 PNG |
| `samples/demo.mp4` | 仓库自带默认样本（约数秒短片） |
| `.env` | API Key 等私密配置（不要发给别人） |

---

## 常见问题

**一直停在「思考并调用工具中…」**  
多半是没开 VPN / 连不上 Gemini。网页约 **20 秒**会提示仍在等待，约 **60 秒**无响应会停止等待并请你重试；也可点 **重试上一条**。先确认 VPN 已开，或检查 `.env` 里的 `HTTPS_PROXY`。

**端口被占用（WinError 10048）**  
7860 已被占用。关掉之前的 `python web_app.py`，或换端口：

```powershell
$env:WEB_PORT=7861; python web_app.py
```

**终端出现 ConnectionResetError / WinError 10054**  
多半是浏览器拖进度条或切换视频时断开连接，**一般可忽略**，不影响剪辑。

**提示 429 / 额度用尽**  
换 `.env` 里的模型后重启，例如：

```env
GEMINI_MODEL=gemini-flash-lite-latest
```

**提示 503 / 模型繁忙**  
等 1～2 分钟再试，或换 lite 模型。

**连不上 Gemini / ConnectError**  
确认 VPN 已开；需要时设置 `HTTPS_PROXY`。

**上传后还在播旧视频**  
再点一次「刷新预览」，或重新上传；仍不对请硬刷新页面（Ctrl+F5）并重启 `web_app.py`。

**中文标题乱码或方框**  
Windows 一般会自动用微软雅黑。不行时在 `.env` 指定字体：

```env
VIDEO_FONT=C:\Windows\Fonts\msyh.ttc
```

**切片时间差一点**  
说「切得更准一点」，会走更精确（稍慢）的重编码。

**上传多大？**  
默认约 100MB，可在 `.env` 用 `WEB_MAX_UPLOAD_MB` 调整。建议先用短视频体验。

---

## 使用注意

- 本机单人体验：没有账号系统，也不适合直接暴露到公网  
- 改文件前一定有「确认」步骤；不确定就先别确认  
- API Key、个人视频不要提交到 Git  
- 依赖在线 Gemini，免费额度有限；网络不稳时回复会变慢  

---

## 下一步可以玩什么

1. 上传一条竖屏短视频，做「去片头 → 加标题 → 截帧」  
2. 切两段再「拼起来」  
3. 试「静音 + 两倍速」看节奏变化  

玩顺之后，若想了解背后的 Agent / 工具调用，再看 [LEARNING.md](./LEARNING.md)。
